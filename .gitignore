# dependencies
node_modules/

# build output (regenerated by `npm run build` / `node scripts/build.mjs`)
# lib/ is committed intentionally? No — keep artifacts out of git; rebuild via build script.
lib/

# macOS
.DS_Store

# logs / editor
*.log
.vscode/
.idea/
